apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: lunchpick
spec:
  selector:
    app: recommendation-service
  ports:
  - name: http
    port: 80
    targetPort: 8082
  type: ClusterIP
