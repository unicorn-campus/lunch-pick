apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: lunchpick
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  rules:
    - host: web.20.249.211.140.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 8080
