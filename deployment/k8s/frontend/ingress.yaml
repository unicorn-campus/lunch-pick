apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: lunchpick
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 8080
