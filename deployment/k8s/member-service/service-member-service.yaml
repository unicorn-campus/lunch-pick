apiVersion: v1
kind: Service
metadata:
  name: member-service
  namespace: lunchpick
spec:
  selector:
    app: member-service
  ports:
  - name: http
    port: 80
    targetPort: 8081
  type: ClusterIP
