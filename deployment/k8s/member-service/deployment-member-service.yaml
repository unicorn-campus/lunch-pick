apiVersion: apps/v1
kind: Deployment
metadata:
  name: member-service
  namespace: lunchpick
spec:
  replicas: 1
  selector:
    matchLabels:
      app: member-service
  template:
    metadata:
      labels:
        app: member-service
    spec:
      imagePullSecrets:
      - name: lunchpick
      containers:
      - name: member-service
        image: hiondal/lunchpick-member-service:dev-20260305132601
        imagePullPolicy: Always
        ports:
        - containerPort: 8081
        envFrom:
        - configMapRef:
            name: cm-common
        - configMapRef:
            name: cm-member-service
        - secretRef:
            name: secret-common
        - secretRef:
            name: secret-member-service
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "1024Mi"
        startupProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8081
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8081
          periodSeconds: 15
          failureThreshold: 3
