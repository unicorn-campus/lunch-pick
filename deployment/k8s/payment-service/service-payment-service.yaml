apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: lunchpick
spec:
  selector:
    app: payment-service
  ports:
  - name: http
    port: 80
    targetPort: 8083
  type: ClusterIP
