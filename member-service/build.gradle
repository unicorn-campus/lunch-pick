// member-service: 포트 8081, Layered Architecture
// Spring Boot 플러그인 및 공통 의존성은 루트 build.gradle에서 적용됨

bootJar {
    archiveFileName = 'member-service.jar'
}

dependencies {
    // Web
    implementation 'org.springframework.boot:spring-boot-starter-web'

    // Security
    implementation 'org.springframework.boot:spring-boot-starter-security'

    // Data JPA + PostgreSQL
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    runtimeOnly 'org.postgresql:postgresql'

    // Redis (세션, JWT 블랙리스트, 취향 프로파일 캐시 — DB 0, DB 1)
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'

    // JWT (공통 모듈의 JwtTokenProvider 사용)
    implementation libs.jjwt.api
    runtimeOnly libs.jjwt.impl
    runtimeOnly libs.jjwt.jackson

    // Validation
    implementation 'org.springframework.boot:spring-boot-starter-validation'

    // Redis Streams MQ 소비는 spring-data-redis(opsForStream) 사용 — Kafka binder 불필요

    // Resilience4j Circuit Breaker (Spring Cloud BOM 관리)
    implementation 'org.springframework.cloud:spring-cloud-starter-circuitbreaker-resilience4j'
}
