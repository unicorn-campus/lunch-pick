# 설계서 ④ 역할계약서 — 런치픽(LunchPick)

**작성**: 플로니(오케스트레이션 엔지니어) · **작성일** 2026-08-11
**입력**: `_shared/프로젝트정보.md`(`V-07`) · `01-목표품질속성카드.md` · `02-논리아키텍처.md` ·
`03-패턴시퀀스설계.md` · 기획 산출물 `docs/plan/`
**미검증 설계** — 실제 권한을 붙여 본 값이 아니라 ②③의 이름과 요구 원문으로 정한 계약임.

이 문서는 **"③의 단계를 몇 명이 나눠 맡고, 그 한 명이 어디까지 손댈 수 있는가"**에 답함.

`R-n`은 담당자 식별자, `S-n`은 내부 저장소, `TB-n`은 Trust Boundary(신뢰 경계), `P-n`은 프로세스이며
`S-n` · `TB-n` · `P-n`은 `02-논리아키텍처.md`가 붙인 번호임. `S-Rn` · `S-Bn` · `S-En`은
`03-패턴시퀀스설계.md`가 붙인 단계 식별자임.

**이 문서가 소유하는 값** — 담당자 수와 묶음 · 저장소·도구의 논리 이름과 권한 등급 · 사람 담당자 배정 ·
승인자 짝짓기 · 사용 모델.
**이 문서가 소유하지 않는 값** — 단계ID · State 필드 이름 · 전달 집합 키 이름 · 타임아웃 · 재시도 ·
모델 호출 횟수(③ 소유, 인용만 함) · 표·열·커넥터 규격·민감 필드 식별자(⑤ 소유, 여기 없음) ·
비용 상한(⑥ 소유).

---

## 1. 담당자 명부

| `R-n` | 이름 | 종류 | 맡는 단계 | 판정 근거 1줄 |
|-------|------|------|----------|--------------|
| `R-1` | 추천·설명 문장 생성기 | 에이전트 | `오늘의 추천 조회 · S-R10` / `추천 이유 상세 확인 · S-R24` / `취향 인사이트 조회 · S-R55` / `일일 취향 학습 배치 · S-B9` | ③에서 `1회` 이상인 4단계가 같은 자료(이력·집계·스냅샷)를 보고 같은 출력 검사 규칙을 지므로 한 묶음임 |
| `R-2` | 취향 임베딩 생성기 | 에이전트 | `일일 취향 학습 배치 · S-B6` / `온보딩 완료 초기 프로파일 생성 · S-E5` | 문장 생성 묶음과 컨텍스트가 섞이면 문장 규칙과 벡터 규칙을 함께 끌고 다니므로 `Q1`이 `예`임 |
| `R-3` | 추천 응답 조립 처리기 | 처리기 | `오늘의 추천 조회 · S-R2` ~ `S-R9` · `S-R11` ~ `S-R13` / `추천 이유 상세 확인 · S-R22` · `S-R23` · `S-R25` · `S-R26` / `추천 수락·식사 기록·피드백 · S-R32` · `S-R33` / `취향 인사이트 조회 · S-R52` ~ `S-R54` · `S-R56` · `S-R57` | 읽기와 되돌릴 수 있는 쓰기만 있고 외부 조회 도구(TB-3·TB-4)를 부르는 유일한 묶음임 |
| `R-4` | 식사 기록·피드백 처리기 | 처리기 | `추천 수락·식사 기록·피드백 · S-R36` ~ `S-R38` · `S-R40` ~ `S-R42` / `기억 제한 도달 구독 안내 · S-E11` | 외부 서비스 도구 0건이고 `S-3` 이력 저장소 쓰기만 갖는 권한 경계임 |
| `R-5` | 알림 본문 조립·조건 판정 처리기 | 처리기 | `알림 발송 배치 · S-B21` ~ `S-B25` · `S-B27` | 알림 채널 정보(푸시 토큰 참조·미리보기 본문)를 다루는 유일한 결정론 묶음이라 경계를 뗌 |
| `R-6` | 취향 벡터 커밋 처리기 | 처리기 | `일일 취향 학습 배치 · S-B1` ~ `S-B5` · `S-B7` · `S-B8` · `S-B10` ~ `S-B12` / `온보딩 완료 초기 프로파일 생성 · S-E6` | `S-4` 취향 벡터 인덱스에 쓰는 권한을 한 담당자로 모아 직전 1세대 보존 규칙을 한 곳에 둠 |
| `R-7` | 구독·해지 상태 처리기 | 처리기 | `구독 결제 · S-R62` ~ `S-R64` · `S-R67` ~ `S-R71` / `구독 해지 예약 · S-R82` · `S-R83` · `S-R85` ~ `S-R87` | `S-5` 구독·결제 저장소 쓰기 경계이며 되돌릴 수 없는 외부 호출은 갖지 않음 |
| `R-8` | 온보딩 입력검사·동의 등록 처리기 | 처리기 | `온보딩 완료 초기 프로파일 생성 · S-E1` ~ `S-E4` · `S-E7` | `S-1` 회원 저장소 동의·식이제한 쓰기 경계임 |
| `R-9` | 구독 안내 표시 처리기 | 처리기 | `기억 제한 도달 구독 안내 · S-E12` ~ `S-E17` | 회원 저장소 쓰기 없이 감사 기록 쓰기와 유예 갱신 호출만 갖는 다른 권한 경계임 |
| `R-10` | 정기 결제 등록 처리기 | 처리기 | `구독 결제 · S-R66` | `쓰기(되돌림 불가)`이므로 반드시 따로 뗌 |
| `R-11` | 알림 발송 호출 처리기 | 처리기 | `알림 발송 배치 · S-B26` | 되돌릴 수 없는 쓰기가 결제와 다른 종류이므로 둘로 뗌 |
| `R-12` | 구독·동의 승인자 | 사람 | `구독 결제 · S-R65`(실행 소유) / `구독 해지 예약 · S-R84`(실행 소유) / `구독 결제 · S-R66`(사전 승인) / `알림 발송 배치 · S-B26`(사전 승인, 동의 갈음) | ③이 사람 응답을 실시간으로 기다리는 단계 2건과 되돌릴 수 없는 쓰기 2건의 사전 승인을 한 등급으로 묶음 |
| `R-13` | 실행 중단 담당자 | 사람 | `오늘의 추천 조회 · S-R8` · `S-R10` / `추천 이유 상세 확인 · S-R24` / `취향 인사이트 조회 · S-R55` / `구독 결제 · S-R66` / `일일 취향 학습 배치 · S-B6` · `S-B8` · `S-B9` / `알림 발송 배치 · S-B26` / `온보딩 완료 초기 프로파일 생성 · S-E5` · `S-E6` | ③의 `진행 중` 개입 11지점은 저장소 쓰기 0건의 중단 권한이라 재정의 등급과 나눔 |
| `R-14` | 품질 재정의·감사 담당자 | 사람 | `오늘의 추천 조회 · S-R11` / `추천 이유 상세 확인 · S-R25` / `추천 수락·식사 기록·피드백 · S-R37` · `S-R38` / `취향 인사이트 조회 · S-R56` / `구독 결제 · S-R66` / `구독 해지 예약 · S-R85` / `일일 취향 학습 배치 · S-B7` · `S-B10` / `알림 발송 배치 · S-B25` · `S-B26` / `기억 제한 도달 구독 안내 · S-E14` | ③의 `사후` 개입 12지점은 State 재정의 쓰기 권한이며 승인자와 같은 사람을 두면 통제가 성립하지 않음 |

**실행 소유 배정은 에이전트·처리기 행과 `R-12`의 실행 소유 2단계로만 셈** — 사람 행의 나머지 항목은
③의 인간 개입 지점을 겹쳐 맡는 자리임(가이드 2-2의 "위 담당자에 더해").

**`담당자 없음`으로 표시한 단말 구간 10단계** — `오늘의 추천 조회 · S-R1` · `S-R14` /
`추천 이유 상세 확인 · S-R21` / `추천 수락·식사 기록·피드백 · S-R31` · `S-R34` · `S-R35` · `S-R39` /
`취향 인사이트 조회 · S-R51` / `구독 결제 · S-R61` / `구독 해지 예약 · S-R81`.
③이 `TB-1` 단말 구간 또는 `TB-8` 단말 구간으로 두고 예산 밖으로 뺀 단계임.

---

## 2. 담당자별 계약

### `R-1` 추천·설명 문장 생성기

- **맡는 단계** — `오늘의 추천 조회 · S-R10` / `추천 이유 상세 확인 · S-R24` /
  `취향 인사이트 조회 · S-R55` / `일일 취향 학습 배치 · S-B9` 〔③ 워크플로우별 설계의 `모델 호출` 열〕
- **책임** — 그 단계가 가리키는, 확신 스코어나 인용 수치가 붙은 사용자용 자연어 문장 묶음 1건 산출
  〔US:UFR-REC-020#시나리오〕
- **사용 정보**
  - 정형 — `S-3` 이력 저장소 `읽기` · `S-6` 추천 캐시 `읽기` 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `S-4` 취향 벡터 인덱스 `읽기` 〔② 내부 저장소 목록〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `TB-2` (E)LLM API 호출 `읽기` 〔② 신뢰경계표 TB-2〕
  - `TB-3` (E)지도 API 호출 도구 `0건` — 후보 식당 조회는 `R-3`이 끝낸 뒤 넘김 〔③ `오늘의 추천 조회 · S-R6`〕
  - 저장소 쓰기 도구 `0건` — 생성과 저장을 갈라 둠 〔③ 판정 기록 4의 판단·실행 쪼갠 짝〕
  - `쓰기(되돌림 불가)` 도구 `0건` 〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **사용 모델** — `claude-opus-5 · 사고 켬(적응형, 모델 기본값) · 확인일 2026-08-11`
  〔BM:비용구조#LLMAPI비용, 설계판단(V-07 1행 + 모델 문서 확인)〕
- **성공 기준** — 나가는 추천마다 이유 1줄·확신 스코어·컨텍스트 태그 3필드가 채워진 비율 100%
  (누락 0건), 추천 조회 경로의 응답이 3,000ms(p95) 안에 닿음 〔US:UFR-REC-020#검증요구사항,
  US:NFR-SYS-010#체크리스트〕
- **중단 조건**
  - 확신 스코어가 빈 신호 (`오늘의 추천 조회 · S-R10 종료 직후`) 〔US:UFR-REC-030#검증요구사항〕
  - 이유 1줄·컨텍스트 태그 중 빈 칸이 있는 신호 (`추천 이유 상세 확인 · S-R24 종료 직후`)
    〔US:UFR-REC-020#처리결과〕
  - 요약 문장이 인용한 수치가 집계값과 어긋난 신호 (`취향 인사이트 조회 · S-R55 종료 직후`)
    〔US:UFR-REC-120#검증요구사항〕
  - 반영 메시지가 인용한 피드백 참조가 빈 신호 (`일일 취향 학습 배치 · S-B9 종료 직후`)
    〔US:UFR-REC-100#검증요구사항〕
  - `TB-2` 응답이 ③이 정한 상한 안에 오지 않는 신호 (`오늘의 추천 조회 · S-R10 종료 직후`)
    〔③ `오늘의 추천 조회` 단계별 설계의 타임아웃 열을 인용함〕

### `R-2` 취향 임베딩 생성기

- **맡는 단계** — `일일 취향 학습 배치 · S-B6` / `온보딩 완료 초기 프로파일 생성 · S-E5`
  〔③ 워크플로우별 설계의 `모델 호출` 열〕
- **책임** — 회원 1명의 갱신 후보 취향 벡터 1건 산출 〔US:UFR-REC-100#시나리오〕
- **사용 정보**
  - 정형 — `S-3` 이력 저장소 `읽기` 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `S-4` 취향 벡터 인덱스 `읽기` 〔② 내부 저장소 목록〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `TB-2` (E)LLM API 호출 `읽기` 〔② 신뢰경계표 TB-2〕
  - `S-4` 커밋 도구 `0건` — 커밋은 `R-6`이 함 〔③ `일일 취향 학습 배치 · S-B8`〕
  - `쓰기(되돌림 불가)` 도구 `0건` 〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **사용 모델** — `claude-opus-5 · 사고 켬(적응형, 모델 기본값) · 확인일 2026-08-11`
  〔BM:비용구조#LLMAPI비용, 설계판단(V-07 1행 + 모델 문서 확인)〕
- **성공 기준** — 갱신 후보 벡터마다 변화량 값이 채워짐(빈 값 0건), 온보딩 회신의 선호 카테고리
  Top 3가 만들어짐 〔US:UFR-REC-100#검증요구사항, US:UFR-MBR-020#처리결과〕
- **중단 조건**
  - 갱신 후보 벡터의 변화량이 빈 신호 (`일일 취향 학습 배치 · S-B6 종료 직후`)
    〔US:UFR-REC-100#처리결과〕
  - 스와이프 결과가 온보딩 완료 조건(카드 7장) 미만인 신호
    (`온보딩 완료 초기 프로파일 생성 · S-E5 진입 직전`) 〔US:UFR-MBR-020#입력요구사항〕
  - 제외 식재료 코드 매핑이 실패로 표시된 신호 (`온보딩 완료 초기 프로파일 생성 · S-E5 진입 직전`)
    〔US:UFR-MBR-040#검증요구사항〕

### `R-3` 추천 응답 조립 처리기

- **맡는 단계** — `오늘의 추천 조회 · S-R2` ~ `S-R9` · `S-R11` ~ `S-R13` /
  `추천 이유 상세 확인 · S-R22` · `S-R23` · `S-R25` · `S-R26` /
  `추천 수락·식사 기록·피드백 · S-R32` · `S-R33` /
  `취향 인사이트 조회 · S-R52` ~ `S-R54` · `S-R56` · `S-R57` 〔③ 워크플로우별 설계〕
- **책임** — 요청 1건에 대한 화면 응답 묶음 1건 산출 〔US:UFR-REC-010#처리결과〕
- **사용 정보**
  - 정형 — `S-3` 이력 저장소 `쓰기(되돌림 가능)` · `S-6` 추천 캐시 `쓰기(되돌림 가능)`
    〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `S-4` 취향 벡터 인덱스 `읽기` 〔② 내부 저장소 목록〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `P-1` 회원업무 사전 조건·구독 상태 조회 `읽기`(내부) 〔③ `오늘의 추천 조회 · S-R3`,
    `취향 인사이트 조회 · S-R53`〕
  - `P-2` 이력업무 집계 통계 조회 `읽기`(내부) 〔③ `취향 인사이트 조회 · S-R54`〕
  - `TB-3` (E)지도 API 조회 `읽기` 〔② 신뢰경계표 TB-3〕
  - `TB-4` (E)날씨 API 조회 `읽기` 〔② 신뢰경계표 TB-4〕
  - `TB-2` (E)LLM API 호출 도구 `0건` — 문장 생성은 `R-1`이 함 〔③ 판정 기록 4의 모델 호출 판정 요약〕
  - `TB-6` 결제 도구 · `TB-7` 발송 도구 `0건` — 조회 경로에 되돌릴 수 없는 호출을 두지 않음
    〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 하드 필터를 통과한 후보만 응답에 오름(위반 노출 0건), 추천 조회 응답이
  3,000ms(p95) 안에 닿음, 상세·인사이트 응답 목표는 `[확인필요: 추천 이유 상세·인사이트 조회 응답 목표]`
  〔US:UFR-MBR-040#검증요구사항, US:NFR-SYS-010#체크리스트〕
- **중단 조건**
  - 위치 동의·좌표 형식을 확정하지 못한 신호 (`오늘의 추천 조회 · S-R3 종료 직후`)
    〔US:UFR-MBR-030#처리결과〕
  - 하드 필터 통과 후보가 0건인 신호 (`오늘의 추천 조회 · S-R8 종료 직후`)
    〔US:UFR-MBR-040#검증요구사항〕
  - 확신 스코어가 임계 미만이거나 설명 3요소에 빈 칸이 있는 신호
    (`오늘의 추천 조회 · S-R11 종료 직후`) 〔US:UFR-REC-030#검증요구사항〕
  - 상세 이유에 민감정보 원문·의료 단정·광고성 문구가 섞인 신호
    (`추천 이유 상세 확인 · S-R25 종료 직후`) 〔US:NFR-SYS-030#체크리스트〕
  - 보존 기간 판정이 무료 30일 경계를 넘긴 조회 신호 (`취향 인사이트 조회 · S-R53 종료 직후`)
    〔US:UFR-REC-110#검증요구사항〕
  - 요약 문장의 수치가 집계값과 어긋난 신호 (`취향 인사이트 조회 · S-R56 종료 직후`)
    〔US:UFR-REC-120#검증요구사항〕

### `R-4` 식사 기록·피드백 처리기

- **맡는 단계** — `추천 수락·식사 기록·피드백 · S-R36` ~ `S-R38` · `S-R40` ~ `S-R42` /
  `기억 제한 도달 구독 안내 · S-E11` 〔③ 워크플로우별 설계〕
- **책임** — 식사 1건에 대한 확정 기록 1건 산출 〔US:UFR-REC-070#처리결과〕
- **사용 정보**
  - 정형 — `S-3` 이력 저장소 `쓰기(되돌림 가능)` 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `0건` 〔설계판단(기록 경로가 벡터 인덱스를 보지 않음)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `P-2` 추천업무 학습 데이터 전달 `쓰기(되돌림 가능)`(내부)
    〔③ `추천 수락·식사 기록·피드백 · S-R41`〕
  - `P-1` 회원업무 기억 제한 도달 신호 전달 `쓰기(되돌림 가능)`(내부)
    〔③ `기억 제한 도달 구독 안내 · S-E11`〕
  - 기록 삭제 도구 `0건` — 30초 취소·수정과 삭제는 다른 일임 〔US:UFR-REC-080#검증요구사항〕
  - `TB-7` 발송 도구 `0건` — 리마인더 예약만 등록하고 발송은 `R-11`이 함
    〔③ `추천 수락·식사 기록·피드백 · S-R42`〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 중복 기록 확정 0건, 기록 확정 건마다 30초 취소 창이 열림, 피드백 저장 시
  컨텍스트 스냅샷이 함께 남음 〔US:UFR-REC-070#검증요구사항, US:UFR-REC-080#검증요구사항,
  ES:04#학습데이터전달〕
- **중단 조건**
  - 같은 식사 1건이 이미 기록된 신호 (`추천 수락·식사 기록·피드백 · S-R36 종료 직후`)
    〔US:UFR-REC-070#처리결과〕
  - 기록 시각이 식사 시간대(10:30 ~ 15:00) 밖인 신호
    (`추천 수락·식사 기록·피드백 · S-R36 종료 직후`) 〔US:UFR-REC-070#검증요구사항〕
  - 30초 취소 창이 닫힌 뒤 취소 요청이 들어온 신호
    (`추천 수락·식사 기록·피드백 · S-R38 종료 직후`) 〔US:UFR-REC-080#처리결과〕
  - 누적 기록 수를 읽지 못한 신호 (`기억 제한 도달 구독 안내 · S-E11 진입 직전`)
    〔US:UFR-REC-110#검증요구사항〕

### `R-5` 알림 본문 조립·조건 판정 처리기

- **맡는 단계** — `알림 발송 배치 · S-B21` ~ `S-B25` · `S-B27` 〔③ `W-8 알림 발송 배치`〕
- **책임** — 회원 1명에 대한 발송 가부 판정이 붙은 알림 본문 1건 산출
  〔US:UFR-REC-090#검증요구사항〕
- **사용 정보**
  - 정형 — `S-3` 이력 저장소 `읽기` · `S-6` 추천 캐시 `읽기` · `S-7` 감사 로그 저장소
    `쓰기(되돌림 가능)` 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `0건` 〔설계판단(본문은 직전 추천 이유를 인용하고 새로 만들지 않음)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `P-1` 회원업무 알림 설정 조회 `읽기`(내부) 〔③ `알림 발송 배치 · S-B22`〕
  - `TB-7` (E)푸시·알림 발송 채널 호출 도구 `0건` — 되돌릴 수 없는 발송은 `R-11`만 부름
    〔③ `알림 발송 배치 · S-B26`〕
  - `TB-2` (E)LLM API 호출 도구 `0건` — 본문을 새로 만들지 않고 인용함
    〔③ `알림 발송 배치` 최악 모델 호출 주석〕
  - 재발송 도구 `0건` — 1일 상한과 리마인더 1회 규정을 재발송이 깨뜨림
    〔US:UFR-REC-090#검증요구사항〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 알림 설정 ON 확인과 1일 상한 검사를 통과한 회원만 발송 대상으로 넘김,
  리마인더는 회원 1명당 1회로 그침 〔US:UFR-REC-090#검증요구사항, US:UFR-MBR-050#입력요구사항〕
- **중단 조건**
  - 알림 설정이 OFF인 신호 (`알림 발송 배치 · S-B23 진입 직전`) 〔US:UFR-MBR-050#입력요구사항〕
  - 1일 상한이 소진된 신호 (`알림 발송 배치 · S-B23 종료 직후`) 〔US:UFR-REC-090#검증요구사항〕
  - 리마인더가 이미 1회 나간 신호 (`알림 발송 배치 · S-B23 종료 직후`)
    〔US:UFR-REC-090#검증요구사항〕
  - 본문에 민감정보 원문·식이제한 위반 언급이 섞인 신호 (`알림 발송 배치 · S-B25 종료 직후`)
    〔US:NFR-SYS-030#체크리스트〕
  - 발송 대상자 목록을 확정하지 못한 신호 (`알림 발송 배치 · S-B22 종료 직후`)
    〔설계판단(③ `S-B22` 초과 시 처리)〕

### `R-6` 취향 벡터 커밋 처리기

- **맡는 단계** — `일일 취향 학습 배치 · S-B1` ~ `S-B5` · `S-B7` · `S-B8` · `S-B10` ~ `S-B12` /
  `온보딩 완료 초기 프로파일 생성 · S-E6` 〔③ 워크플로우별 설계〕
- **책임** — 회원 1명에 대한 취향 벡터 커밋 결과 1건 산출 〔US:UFR-REC-100#처리결과〕
- **사용 정보**
  - 정형 — `S-3` 이력 저장소 `읽기` · `S-7` 감사 로그 저장소 `쓰기(되돌림 가능)`
    〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `S-4` 취향 벡터 인덱스 `쓰기(되돌림 가능)` 〔② 내부 저장소 목록, ② ADR-5〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `P-2` 이력업무 누적 이력·피드백 조회 `읽기`(내부) 〔③ `일일 취향 학습 배치 · S-B2` · `S-B4`〕
  - `P-2` 추천업무 배치 완료 신호 전달 `쓰기(되돌림 가능)`(내부)
    〔③ `일일 취향 학습 배치 · S-B12`〕
  - `TB-2` (E)LLM API 호출 도구 `0건` — 임베딩 생성은 `R-2`가 함
    〔③ 판정 기록 4의 모델 호출 판정 요약〕
  - 이전 세대 삭제 도구 `0건` — 직전 1세대 보존이 되돌리기 수단임 〔② ADR-5〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 품질 임계를 통과한 회원만 커밋되고 임계 미달 회원은 이전 벡터를 유지함(임계 미달
  커밋 0건), 전일 피드백 보유 회원 대비 갱신 성공 비율은
  `[확인필요: 일일 학습 반영률 목표값]` 〔US:UFR-REC-100#검증요구사항, ES:05#자가검증〕
- **중단 조건**
  - 전일 피드백 대상자 목록이 빈 신호 (`일일 취향 학습 배치 · S-B2 종료 직후`)
    〔US:UFR-REC-100#처리결과〕
  - 품질 임계(전일 수락률 · 만족 피드백 비율) 미달 신호
    (`일일 취향 학습 배치 · S-B7 종료 직후`) 〔US:UFR-REC-100#검증요구사항〕
  - 직전 1세대 보존본을 확인하지 못한 신호 (`일일 취향 학습 배치 · S-B8 진입 직전`)
    〔설계판단(② ADR-5)〕
  - 반영 메시지 검사가 불일치로 판정된 신호 (`일일 취향 학습 배치 · S-B10 종료 직후`)
    〔US:UFR-REC-100#검증요구사항〕
  - 초기 벡터 커밋의 중복 방지 키가 빈 신호
    (`온보딩 완료 초기 프로파일 생성 · S-E6 진입 직전`) 〔설계판단(③ `W-9` 재개 방안)〕

### `R-7` 구독·해지 상태 처리기

- **맡는 단계** — `구독 결제 · S-R62` ~ `S-R64` · `S-R67` ~ `S-R71` /
  `구독 해지 예약 · S-R82` · `S-R83` · `S-R85` ~ `S-R87` 〔③ 워크플로우별 설계〕
- **책임** — 회원 1명에 대한 구독 상태 반영 결과 1건 산출 〔US:UFR-PAY-020#처리결과〕
- **사용 정보**
  - 정형 — `S-5` 구독·결제 저장소 `쓰기(되돌림 가능)` · `S-7` 감사 로그 저장소
    `쓰기(되돌림 가능)` 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `0건` 〔설계판단(결제 경로가 벡터 인덱스를 보지 않음)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `P-1` 회원업무 구독 상태 갱신 `쓰기(되돌림 가능)`(내부) 〔③ `구독 결제 · S-R68`〕
  - `P-2` 이력업무 보관 기간 해제 `쓰기(되돌림 가능)`(내부) 〔③ `구독 결제 · S-R69`〕
  - `TB-6` (E)결제 게이트웨이 호출 도구 `0건` — 되돌릴 수 없는 등록은 `R-10`만 부름
    〔③ `구독 결제 · S-R66`〕
  - 결제 취소·환불 도구 `0건` — 청약철회는 별도 절차이며 이 담당자가 걸고 끊게 두지 않음
    〔ES:규제반영표#전자상거래법〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 결제 진행 건마다 청약철회 7일·자동 갱신·해지 방법 고지가 먼저 표시됨(고지 없이
  진행 0건), 해지는 기간 종료 시점 전환으로만 예약됨(즉시 해지 0건)
  〔US:UFR-PAY-020#검증요구사항, US:UFR-PAY-030#처리결과〕
- **중단 조건**
  - 결제 수단 유효성·금액 정합성 중 하나가 어긋난 신호 (`구독 결제 · S-R63 종료 직후`)
    〔US:UFR-PAY-020#처리결과〕
  - 청약철회 7일·자동 갱신·해지 방법 고지가 표시되지 않은 신호 (`구독 결제 · S-R64 종료 직후`)
    〔US:UFR-PAY-020#검증요구사항〕
  - 결제 결과 저장이 실패한 신호 (`구독 결제 · S-R67 종료 직후`) 〔US:UFR-PAY-020#처리결과〕
  - 해지 가능 조건(구독 상태 · 남은 기간)을 확정하지 못한 신호
    (`구독 해지 예약 · S-R82 종료 직후`) 〔US:UFR-PAY-030#처리결과〕
  - 확인 플래그가 빈 신호 (`구독 해지 예약 · S-R85 진입 직전`) 〔US:UFR-PAY-030#시나리오〕

### `R-8` 온보딩 입력검사·동의 등록 처리기

- **맡는 단계** — `온보딩 완료 초기 프로파일 생성 · S-E1` ~ `S-E4` · `S-E7`
  〔③ `W-9 온보딩 완료 초기 프로파일 생성`〕
- **책임** — 회원 1명에 대한 동의·식이제한 코드 등록 결과 1건 산출 〔US:UFR-MBR-040#처리결과〕
- **사용 정보**
  - 정형 — `S-1` 회원 저장소 `쓰기(되돌림 가능)` · `S-7` 감사 로그 저장소 `쓰기(되돌림 가능)`
    〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `0건` 〔설계판단(등록 경로가 벡터 인덱스를 보지 않음)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `P-2` 추천업무 스와이프 결과·제외 식재료 코드 전달 `쓰기(되돌림 가능)`(내부)
    〔③ `온보딩 완료 초기 프로파일 생성 · S-E4`〕
  - `TB-2` (E)LLM API 호출 도구 `0건` — 알레르기 원문을 모델 벤더로 넘기지 않음
    〔② 경계 미통과 항목〕
  - `TB-5` (E)카카오 로그인 호출 도구 `0건` — 인증 흐름은 ③의 워크플로우 밖임 〔③ 부속 표 대조 기록〕
  - 동의 이력 삭제 도구 `0건` — 철회 기록도 남겨야 함 〔US:UFR-MBR-030#검증요구사항〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 동의 이력이 저장되지 않은 상태에서 해당 도구 호출이 진행된 건 0건, 알레르기
  직접 입력의 코드 매핑 실패 건은 페일세이프로 그 식재료를 후보에서 지움
  〔US:UFR-MBR-030#검증요구사항, US:UFR-MBR-040#검증요구사항〕
- **중단 조건**
  - 알레르기 직접 입력이 길이·허용문자 기준을 벗어난 신호
    (`온보딩 완료 초기 프로파일 생성 · S-E2 종료 직후`) 〔US:UFR-MBR-040#입력요구사항〕
  - 식재료 코드 매핑이 실패한 신호 (`온보딩 완료 초기 프로파일 생성 · S-E2 종료 직후`)
    〔US:UFR-MBR-040#검증요구사항〕
  - 동의 이력 저장이 실패한 신호 (`온보딩 완료 초기 프로파일 생성 · S-E3 종료 직후`)
    〔US:UFR-MBR-030#검증요구사항〕
  - 스와이프 결과가 카드 7장 미만인 신호 (`온보딩 완료 초기 프로파일 생성 · S-E1 종료 직후`)
    〔US:UFR-MBR-020#입력요구사항〕

### `R-9` 구독 안내 표시 처리기

- **맡는 단계** — `기억 제한 도달 구독 안내 · S-E12` ~ `S-E17`
  〔③ `W-10 기억 제한 도달 구독 안내`〕
- **책임** — 회원 1명에 대한 구독 안내 표시 결과 1건 산출 〔US:UFR-PAY-010#처리결과〕
- **사용 정보**
  - 정형 — `S-5` 구독·결제 저장소 `읽기` · `S-3` 이력 저장소 `읽기` · `S-7` 감사 로그 저장소
    `쓰기(되돌림 가능)` 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `0건` 〔설계판단(안내 문구가 고정 템플릿임)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `P-2` 이력업무 삭제 유예 상태 갱신 `쓰기(되돌림 가능)`(내부)
    〔③ `기억 제한 도달 구독 안내 · S-E17`〕
  - `P-4` 프론트엔드 안내 화면 표시 `쓰기(되돌림 가능)`(내부)
    〔③ `기억 제한 도달 구독 안내 · S-E15`〕
  - `TB-2` (E)LLM API 호출 도구 `0건` — 문구는 고정 템플릿에 집계 수치를 끼움
    〔③ `W-10` 최악 모델 호출 주석〕
  - 이력 삭제 도구 `0건` — 유예 상태 갱신과 삭제는 다른 일임 〔US:UFR-REC-110#검증요구사항〕
  - `TB-6` 결제 도구 `0건` — 안내는 결제를 걸지 않음 〔ES:07#구독안내표시〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 같은 회원·같은 도달 일자에 안내가 두 번 표시된 건 0건, 안내 문구의 수치가
  집계값과 일치할 때만 화면에 오름 〔US:UFR-PAY-010#검증요구사항, US:UFR-REC-120#검증요구사항〕
- **중단 조건**
  - 구독 상태·기안내 이력을 확인하지 못한 신호 (`기억 제한 도달 구독 안내 · S-E12 종료 직후`)
    〔US:UFR-PAY-010#시나리오〕
  - 안내 문구의 수치가 집계값과 어긋난 신호 (`기억 제한 도달 구독 안내 · S-E14 종료 직후`)
    〔US:UFR-REC-120#검증요구사항〕
  - 삭제 유예 상태 갱신이 실패한 신호 (`기억 제한 도달 구독 안내 · S-E17 종료 직후`)
    〔US:UFR-REC-110#검증요구사항〕

### `R-10` 정기 결제 등록 처리기

- **맡는 단계** — `구독 결제 · S-R66` 〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **책임** — 회원 1명에 대한 정기 결제 등록 1건 산출 〔US:UFR-PAY-020#처리결과〕
- **사용 정보**
  - 정형 — `0건` — `K-31`을 받고 `K-32`를 내놓기만 함 〔③ `W-5` 단계 간 전달 집합〕
  - 의미 정보(RAG) — `0건` 〔설계판단(결제 호출이 인덱스를 보지 않음)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `TB-6` (E)결제 게이트웨이 정기 결제 등록 `쓰기(되돌림 불가)` — ③ `구독 결제 · S-R65`
    사전 승인(승인자: `R-12` 구독·동의 승인자) 〔ES:07#정기결제등록, US:UFR-PAY-020#검증요구사항〕
  - 결제 취소·환불 도구 `0건` — 걸고 끊는 권한을 한 담당자에 두지 않음
    〔ES:규제반영표#전자상거래법〕
  - 저장소 쓰기 도구 `0건` — 결과 저장은 `R-7`이 함 〔③ `구독 결제 · S-R67`〕
  - `TB-7` 발송 도구 `0건` 〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 명시 승인을 통과한 요청만 등록 호출로 나감(승인 없는 호출 0건), 같은 멱등성 키로
  중복 등록된 건 0건 〔US:UFR-PAY-020#검증요구사항, ES:07#정기결제등록〕
- **중단 조건**
  - 명시 승인이 통과되지 않은 신호 (`구독 결제 · S-R66 진입 직전`)
    〔US:UFR-PAY-020#검증요구사항〕
  - 멱등성 키가 빈 신호 (`구독 결제 · S-R66 진입 직전`) 〔설계판단(V-07 4행의 멱등성 키)〕
  - `TB-6`이 등록 실패를 회신한 신호 (`구독 결제 · S-R66 종료 직후`) — 자동 재시도 없이 멈춤은
    ③이 정한 값을 인용함 〔US:UFR-PAY-020#처리결과〕

### `R-11` 알림 발송 호출 처리기

- **맡는 단계** — `알림 발송 배치 · S-B26` 〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **책임** — 회원 1명에 대한 알림 발송 1건 산출 〔US:UFR-REC-090#처리결과〕
- **사용 정보**
  - 정형 — `0건` — `K-57`을 받고 `K-58`을 내놓기만 함 〔③ `W-8` 단계 간 전달 집합〕
  - 의미 정보(RAG) — `0건` 〔설계판단(발송 호출이 인덱스를 보지 않음)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - `TB-7` (E)푸시·알림 발송 채널 발송 `쓰기(되돌림 불가)` — ③ `알림 발송 배치 · S-B26`
    사전 승인(알림 설정 ON 동의로 갈음, 승인자: `R-12` 구독·동의 승인자). 동의 확인 단계는
    ③ `알림 발송 배치 · S-B23`임 〔US:UFR-REC-090#검증요구사항, ES:04#리마인더〕
  - 재발송 도구 `0건` — 1일 상한과 리마인더 1회 규정을 재발송이 깨뜨림
    〔US:UFR-REC-090#검증요구사항〕
  - 저장소 쓰기 도구 `0건` — 발송 기록은 `R-5`가 함 〔③ `알림 발송 배치 · S-B27`〕
  - `TB-6` 결제 도구 `0건` 〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **사용 모델** — `모델 미사용(결정론적 실행)` 〔③ 판정 기록 4의 모델 호출 판정 요약(`0회`)〕
- **성공 기준** — 발송 조건 판정과 본문 출력 검사를 모두 통과한 건만 발송됨(미통과 발송 0건),
  같은 중복 방지 키로 두 번 나간 건 0건 〔US:UFR-REC-090#검증요구사항, ③ `W-8` 재개 방안〕
- **중단 조건**
  - 발송 조건 판정이 통과하지 않은 신호 (`알림 발송 배치 · S-B26 진입 직전`)
    〔US:UFR-REC-090#검증요구사항〕
  - 본문 출력 검사가 차단으로 판정된 신호 (`알림 발송 배치 · S-B26 진입 직전`)
    〔US:NFR-SYS-030#체크리스트〕
  - 중복 방지 키가 빈 신호 (`알림 발송 배치 · S-B26 진입 직전`) 〔설계판단(③ `W-8` 재개 방안)〕
  - `TB-7`이 발송 실패를 회신한 신호 (`알림 발송 배치 · S-B26 종료 직후`)
    〔US:UFR-REC-090#검증요구사항〕

### `R-12` 구독·동의 승인자

- **맡는 단계** — `구독 결제 · S-R65`(실행 소유) / `구독 해지 예약 · S-R84`(실행 소유) /
  `구독 결제 · S-R66`(사전 승인) / `알림 발송 배치 · S-B26`(사전 승인, 알림 설정 ON 동의로 갈음)
  〔③ 사람 응답을 실시간으로 기다리는 단계, 되돌릴 수 없는 쓰기 단계 목록〕
- **책임** — 되돌릴 수 없는 실행 1건에 대한 승인·반려 판정 1건 산출 〔US:UFR-PAY-020#검증요구사항〕
- **사용 정보**
  - 정형 — `S-5` 구독·결제 저장소 `읽기`(승인 화면에 오른 값 확인) 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `0건` 〔설계판단(승인 판단이 인덱스를 보지 않음)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
  - 승인 판단에 필요한 정보 항목 — 결제 금액 · 결제 주기 · 청약철회 7일 고지 · 자동 갱신 고지 ·
    해지 방법 고지 · 해지 시 남은 기간 · 데이터 보관 안내 · 알림 설정 ON 여부
    〔US:UFR-PAY-020#검증요구사항, US:UFR-PAY-030#검증요구사항, ES:규제반영표#전자상거래법〕
- **사용 도구**
  - `P-4` 프론트엔드 승인·확인 모달 응답 `쓰기(되돌림 가능)`(내부) 〔③ `구독 결제 · S-R65`〕
  - 저장소 직접 쓰기 도구 `0건` — 승인 판정만 남기고 상태 반영은 `R-7`이 함
    〔③ `구독 결제 · S-R67`〕
  - 사후 재정의·감사 도구 `0건` — 자기 승인을 자기가 감사하지 않음(재정의는 `R-14`)
    〔설계판단(가이드 2-5의 승인·감사 분리)〕
- **사용 모델** — `모델 미사용(사람 판단)` 〔③ `구독 결제 · S-R65`의 `모델 호출` 열(`0회`)〕
- **성공 기준** — 되돌릴 수 없는 실행마다 승인 판정이 남음(판정 없는 실행 0건), 승인 화면에
  금액·주기·법정 고지 항목이 빠짐없이 오름 〔US:UFR-PAY-020#검증요구사항,
  ES:규제반영표#전자상거래법〕
- **중단 조건**
  - 금액·주기 재표시가 화면에 오르지 않은 신호 (`구독 결제 · S-R65 진입 직전`)
    〔US:UFR-PAY-020#검증요구사항〕
  - 청약철회 7일·자동 갱신·해지 방법 고지가 빠진 신호 (`구독 결제 · S-R65 진입 직전`)
    〔ES:규제반영표#전자상거래법〕
  - 대기 상한을 넘긴 신호 (`구독 결제 · S-R65 종료 직후`) — 상한 값은 ③이 소유하며 여기서는
    인용함 〔③ 사람 응답을 실시간으로 기다리는 단계〕
  - 보관 안내·남은 기간이 화면에 오르지 않은 신호 (`구독 해지 예약 · S-R84 진입 직전`)
    〔US:UFR-PAY-030#검증요구사항〕

### `R-13` 실행 중단 담당자

- **맡는 단계** — `오늘의 추천 조회 · S-R8` · `S-R10` / `추천 이유 상세 확인 · S-R24` /
  `취향 인사이트 조회 · S-R55` / `구독 결제 · S-R66` /
  `일일 취향 학습 배치 · S-B6` · `S-B8` · `S-B9` / `알림 발송 배치 · S-B26` /
  `온보딩 완료 초기 프로파일 생성 · S-E5` · `S-E6` 〔③ 워크플로우별 설계의 `인간 개입` 열(진행 중)〕
- **책임** — 이상 신호 1건에 대한 실행 중단 판정 1건 산출 〔US:NFR-SYS-030#체크리스트〕
- **사용 정보**
  - 정형 — `S-7` 감사 로그 저장소 `읽기` 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `0건` 〔설계판단(중단 판단이 인덱스를 보지 않음)〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
- **사용 도구**
  - 실행 중단 스위치 `쓰기(되돌림 가능)`(내부) — 흐름을 세우고 재실행 부작용은 ③의 중복 방지 키가
    막음 〔③ `W-7` · `W-8` · `W-9` 재개 방안〕
  - 저장소 쓰기 도구 `0건` — 중단은 값을 바꾸지 않음 〔설계판단(권한 등급 분리)〕
  - 사후 재정의 도구 `0건` — 재정의는 `R-14`가 함 〔설계판단(가이드 2-5의 권한 등급 분리)〕
  - `TB-6` 결제 도구 · `TB-7` 발송 도구 `0건` 〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **사용 모델** — `모델 미사용(사람 판단)` 〔③ 해당 단계의 `모델 호출` 열은 ③이 소유하며 중단
  판정 자체는 사람이 함〕
- **성공 기준** — 중단 판정 건마다 감사 레코드가 남음(기록 없는 중단 0건), 중단 뒤 재실행에서
  중복 커밋·중복 발송 0건. 운영 대응 창은
  `[확인필요: 사후 재정의·진행 중 중단의 운영 대응 창(03:00 배치 포함)]`
  〔③ `W-7` 재개 방안, 설계판단(V-10 29번 도구 호출 감사)〕
- **중단 조건**
  - 하드 필터가 페일세이프로 후보를 지우는 건수가 늘어나는 신호
    (`오늘의 추천 조회 · S-R8 종료 직후`) 〔US:UFR-MBR-040#검증요구사항〕
  - `TB-2` 응답에 민감정보 원문이 섞인 신호 (`오늘의 추천 조회 · S-R10 종료 직후`)
    〔US:NFR-SYS-030#체크리스트〕
  - 커밋 후보 벡터의 변화량이 품질 임계 판정값을 벗어난 신호
    (`일일 취향 학습 배치 · S-B8 진입 직전`) 〔US:UFR-REC-100#검증요구사항〕
  - `TB-6` 등록 호출이 승인 판정 없이 진입한 신호 (`구독 결제 · S-R66 진입 직전`)
    〔US:UFR-PAY-020#검증요구사항〕
  - `TB-7` 발송 호출이 조건 판정 없이 진입한 신호 (`알림 발송 배치 · S-B26 진입 직전`)
    〔US:UFR-REC-090#검증요구사항〕

### `R-14` 품질 재정의·감사 담당자

- **맡는 단계** — `오늘의 추천 조회 · S-R11` / `추천 이유 상세 확인 · S-R25` /
  `추천 수락·식사 기록·피드백 · S-R37` · `S-R38` / `취향 인사이트 조회 · S-R56` /
  `구독 결제 · S-R66` / `구독 해지 예약 · S-R85` / `일일 취향 학습 배치 · S-B7` · `S-B10` /
  `알림 발송 배치 · S-B25` · `S-B26` / `기억 제한 도달 구독 안내 · S-E14`
  〔③ 워크플로우별 설계의 `인간 개입` 열(사후)〕
- **책임** — 산출값 1건에 대한 재정의 판정 1건 산출 〔US:UFR-REC-080#검증요구사항〕
- **사용 정보**
  - 정형 — `S-3` 이력 저장소 `읽기` · `S-5` 구독·결제 저장소 `읽기` · `S-7` 감사 로그 저장소
    `쓰기(되돌림 가능)` 〔② 내부 저장소 목록〕
  - 의미 정보(RAG) — `S-4` 취향 벡터 인덱스 `읽기` 〔② 내부 저장소 목록〕
  - 관계 정보(GraphRAG) — `0건` 〔설계판단(② 저장 절에 그래프DB가 없음)〕
  - 재정의 대상 — ③이 그 단계의 State 재정의 지점으로 사람을 쓰는 주체에 더한 자리이며, 필드
    이름은 ③의 State 구조 표가 소유함. 재정의 사유는 `S-7`에 남김
    〔③ 각 워크플로우 3. State 구조, 설계판단(V-10 29번 도구 호출 감사)〕
- **사용 도구**
  - State 재정의 입력 `쓰기(되돌림 가능)`(내부) — ③이 사람을 쓰는 주체로 더한 지점에만 씀
    〔③ 각 워크플로우 3. State 구조〕
  - `P-2` 이력업무 기록 수정 `쓰기(되돌림 가능)`(내부)
    〔③ `추천 수락·식사 기록·피드백 · S-R38`〕
  - 삭제 도구 `0건` — 재정의는 값을 고치고 기록을 지우지 않음 〔US:UFR-REC-080#검증요구사항〕
  - 사전 승인 도구 `0건` — 승인은 `R-12`가 하며 자기 승인을 자기가 감사하지 않음
    〔설계판단(가이드 2-5의 승인·감사 분리)〕
  - `TB-6` 결제 도구 · `TB-7` 발송 도구 `0건` 〔③ 되돌릴 수 없는 쓰기 단계 목록〕
- **사용 모델** — `모델 미사용(사람 판단)` 〔③ 해당 단계의 `모델 호출` 열은 ③이 소유하며 재정의
  판정 자체는 사람이 함〕
- **성공 기준** — 재정의 건마다 사유 기록이 남음(사유 없는 재정의 0건), 식이제한 위반 노출과
  민감정보 원문 노출 0건. 운영 대응 창은
  `[확인필요: 사후 재정의·진행 중 중단의 운영 대응 창(03:00 배치 포함)]`
  〔US:UFR-MBR-040#검증요구사항, US:NFR-SYS-030#체크리스트〕
- **중단 조건**
  - 확신 스코어 검증 판정이 안전망 대체 결과와 어긋난 신호
    (`오늘의 추천 조회 · S-R11 종료 직후`) 〔US:UFR-REC-030#검증요구사항〕
  - 기록 확정 결과가 사용자 수정 요청과 어긋난 신호
    (`추천 수락·식사 기록·피드백 · S-R37 종료 직후`) 〔US:UFR-REC-080#검증요구사항〕
  - 품질 임계 판정이 커밋 결과와 어긋난 신호 (`일일 취향 학습 배치 · S-B7 종료 직후`)
    〔US:UFR-REC-100#검증요구사항〕
  - 발송 결과가 알림 OFF 전환 뒤에 남은 신호 (`알림 발송 배치 · S-B26 종료 직후`)
    〔US:UFR-REC-090#검증요구사항〕
  - 구독 상태가 청약철회 요청과 어긋난 신호 (`구독 결제 · S-R66 종료 직후`)
    〔ES:규제반영표#전자상거래법〕
  - 감사 레코드 수가 도구 호출 건수와 어긋난 신호 (`구독 결제 · S-R70 종료 직후`)
    〔설계판단(V-10 29번 도구 호출 감사)〕

---

## 3. 변경요청

| 무엇 | 받는 곳 | 사유 |
|------|--------|------|
| `알림 발송 배치 · S-B23` 발송 조건 판정의 `인간 개입` 열에 `사전(동의 확인)`을 표기해 `S-B26`의 승인 짝을 별도 단계로 뗌 | ③ 패턴/시퀀스 `W-8` 단계별 설계 | `S-B26`의 사전 승인이 그 단계 자신에 붙어 있어 승인자 짝짓기가 자기 참조가 됨. 동의 확인을 실제로 하는 단계는 `S-B23`이므로 승인 지점을 그쪽에 두어야 `쓰기(되돌림 불가)`와 승인 단계가 서로 다른 단계로 짝지어짐 |
| `추천 수락·식사 기록·피드백`에 이력업무 측 요청 접수 단계 2개 신설(`S-R35` 뒤 · `S-R39` 뒤) | ③ 패턴/시퀀스 `W-3` 단계별 설계 | 단말 구간 단계가 전달 집합 `K-17`·`K-20`을 그대로 내놓아, 담당자가 없는 자리에서 서버 담당자로 값이 넘어옴. `W-1`(`S-R2`) · `W-2`(`S-R22`) · `W-4`(`S-R52`)에는 접수 단계가 있어 대칭이 깨짐 |

**2건.**

---

## 4. 미확정 항목

| `[확인필요]` 태그 | 무엇이 없나 | 누구에게 되묻나 | 안 오면 무엇이 막히나 |
|------------------|-----------|---------------|---------------------|
| `[확인필요: 일일 학습 반영률 목표값]` | 전일 피드백 보유 회원 대비 갱신 성공 비율의 목표 숫자 | 기획 담당자 · AI 담당 | `R-6`의 성공 기준이 참·거짓으로 갈리지 않아 개발이 단정문 1개로 옮길 수 없음 |
| `[확인필요: 추천 이유 상세·인사이트 조회 응답 목표]` | `W-2` · `W-4` 경로의 응답 목표 숫자 | 기획 담당자 | `R-3`의 성공 기준 중 상세·인사이트 구간의 합격선을 특정 못 함 |
| `[확인필요: 사후 재정의·진행 중 중단의 운영 대응 창(03:00 배치 포함)]` | 사람 담당자가 중단·재정의를 할 수 있는 시간 구간 | 기획 담당자 · DevOps 담당 | 03:00 배치의 진행 중 중단 지점 6개에 사람이 닿을 수 있는지 판정 못 하고 `R-13` · `R-14`의 성공 기준이 닫히지 않음 |

**3건** — 위 표 3행과 같음.
