# 00 반영대조표 — 런치픽(LunchPick) 아키텍처 설계서 7종

**작성**: 클로니(Agentic AI 아키텍트 총괄) · **작성일** 2026-08-12
**대상**: `01-목표품질속성카드.md` ~ `07-배포설계.md` 7종
**입력**: `_shared/프로젝트정보.md`의 `V-10`(규제·안전 점검 32건) · `V-09`(범위 제외 11건)
**미검증 설계** — 설계서 7종에 그 항목이 적혀 있는지를 대조한 결과이며, 구현·실행으로 지켜졌음을
확인한 결과가 아님.

이 문서는 대응 프롬프트가 없는 최종 통합 산출물임. 반영 위치는 `파일명:섹션명` 형식으로 적음.
민감정보 마스킹 규칙(개인정보·계정·시크릿을 원문 그대로 옮기지 않음)을 이 문서에도 적용함.

---

## 1. 점검 항목 반영 대조표

`V-10` 규제·안전 점검 32항목을 1행씩 대조함. 4계열 전부 `0건 아님`으로 재판정된 상태임
(재판정 기록은 `06-가드레일관측설계.md:판정 기록 4`).

| 점검 항목 | 반영 위치(파일명:섹션명) | 반영 여부 |
|----------|----------------------|:--------:|
| 1 정기 결제 등록·최초 결제는 사용자 명시 승인 없이 실행 금지 | `06-가드레일관측설계.md:판정 기록 3 — 승인 지점` · `06:2-3 차단 규칙 W-5 구독 결제` · `03-패턴시퀀스설계.md:W-5 단계별 설계`(사전 승인 단계) | O |
| 2 구독 해지 실행은 확인 모달 통과 후에만 | `06:2-3 차단 규칙 W-6 구독 해지 예약` · `03:W-6 단계별 설계` | O |
| 3 푸시 발송은 알림 설정 ON 확인 + 1일 상한 검사 후에만 | `06:판정 기록 3 — 승인 지점`(제한 장치 5종) · `06:2-3 차단 규칙 W-8` · `06:2-2 비용 상한 W-8` | O |
| 4 카카오톡 공유·알림톡 발송은 사용자 탭 액션에서만 트리거 | `06:2-3 차단 규칙 W-8 알림 발송 배치` · `04-에이전트역할계약서.md:담당자별 계약 R-11`(자율 호출 대상 제외) | O |
| 5 알레르기 하드필터 불확실 시 추천 확정 금지(페일세이프) | `06:2-3 차단 규칙 W-1`·`W-9` · `02-논리아키텍처.md:배치 결정 ADR-3` · `05-지식도구설계.md:1-1 담당자별 허용 열`(고정 쿼리 전용 배정) | O |
| 6 확신 스코어 임계값 미달 추천 노출 금지 | `06:2-1 출력측 검사 W-1`(필드 지정) · `06:2-3 차단 규칙 W-1` | O |
| 7 배치 취향 벡터 갱신은 품질 임계 미달 시 자동 반영 금지 | `06:2-3 차단 규칙 W-7` · `06:1-1 기록 지점 W-7`(`임계통과여부`·`커밋허용여부`) | O |
| 8 식사 기록 확정에 실행취소 창을 보장함 | `06:1-1 기록 지점 W-3`(`취소창개시시각`·`취소·수정결과`) · `03:W-3 단계별 설계`(창 길이는 ③ 소유) | O |
| 9 LLM이 만든 추천 이유를 화면에 내기 전 검사 | `06:2-1 출력측 검사 W-1`·`W-2` | O |
| 10 학습 반영 메시지의 사실 일치 검사 | `06:2-1 출력측 검사 W-7` | O |
| 11 인사이트 요약 문장·수치의 통계 일치 검사 | `06:2-1 출력측 검사 W-4` · `06:1-1 기록 지점 W-4`(`수치일치여부`) | O |
| 12 푸시·알림 본문도 출력 검사 대상에 포함 | `06:2-1 출력측 검사 W-8` | O |
| 13 출력에 민감정보 재노출 금지 | `06:3-1 마스킹`(전역 17행) · `05:3 민감·보존`(`F-1` ~ `F-13` 변환 지점) | O |
| 14 추천 근거와 실제 결과의 일치도 감사 추적 | `06:1-1 기록 지점 W-1`·`W-4` | O |
| 15 외부 지도·식당 API 응답은 데이터로만 취급 | `06:판정 기록 2 — 입력측 태그` · `06:2-3 차단 규칙 W-1` · `05:2 도구(커넥터) C-3` | O |
| 16 날씨 API 응답 문자열도 지시로 해석 금지 | `06:판정 기록 2 — 입력측 태그` · `06:2-3 차단 규칙 W-1` | O |
| 17 알레르기 직접 입력 문자열 검사 | `06:2-3 차단 규칙 W-9` · `05:1-1 정형 접근 금지 컬럼` | O |
| 18 닉네임 자유 입력이 프롬프트에 섞이는 경로 검사 | `02:경계 미통과 항목`(닉네임 → TB-2 차단) · `06:2-3 차단 규칙` | O |
| 19 수동 위치 입력값 검증 | `06:2-3 차단 규칙 W-1` · `06:판정 기록 2 — 입력측 태그` | O |
| 20 거절 사유·피드백 키워드는 고정 라벨만 허용 | `06:2-1 출력측 검사 W-3`(라벨 목록) · `06:2-3 차단 규칙 W-3` | O |
| 21 결제 입력값의 로그·프롬프트 유입 차단 | `02:경계 미통과 항목`(카드 정보) · `05:2 도구(커넥터)`(키 자체 미생성) · `06:3-1 마스킹` | O |
| 22 개인정보 접근 로그 6개월 보관 | `06:3-1 마스킹`(접근 로그 행) · `06:뒤 산출물이 쓸 목록` · `07-배포설계.md:저장소 배치 표 S-7`(별도 인스턴스 6개월) | O |
| 23 위치정보 보유 6개월, 이후 자동 삭제 | `02:내부 저장소 목록 S-2` · `06:뒤 산출물이 쓸 목록` · `07:저장소 배치 표 S-2` · `07:되돌릴 수 없는 데이터 변경` | O |
| 24 동의 이력(위치·건강 민감정보) 기록 | `06:1-1 기록 지점 W-9`(`동의이력레코드ID`·`동의버전`) · `06:2-3 차단 규칙 W-1`(동의 없는 호출 기본 거부) | O |
| 25 이력 보존 정책(무료 30일·프리미엄 무제한·해지 후 제한·결제 실패 7일 유예) | `06:2-3 차단 규칙 W-4`·`W-10` · `07:저장소 배치 표 S-3`(행 단위 파기) | O |
| 26 개인정보 전송요구권 대응 및 내보내기 이력 기록 | `06:뒤 산출물이 쓸 목록`(도구 호출 감사 레코드)에만 있음 | **X** |
| 27 배치 학습 실행 결과 기록 | `06:1-1 기록 지점 W-7` · `06:1-1 루프 구간 기록 항목 W-7` | O |
| 28 피드백과 함께 컨텍스트 스냅샷 보존 | `06:1-1 기록 지점 W-3` · `06:3-1 마스킹 F-9` | O |
| 29 도구 호출 감사 로그 | `06:1-1 기록 지점`(전 쓰기 단계 `감사레코드ID`) · `06:3-1 마스킹 감사 로그 행` · `07:저장소 배치 표 S-7` | O |
| 30 위치정보 보호 책임자 지정 및 처리 기록 책임 주체 명시 | `07:저장 — 운영 책임 주체`(칸만 있고 값이 빔) | **X** |
| 31 청약철회권 7일 보장·자동 갱신 고지·해지 방법 안내 | `06:2-3 차단 규칙 W-5` · `06:1-1 기록 지점 W-5`(`고지버전`) | O |
| 32 식당 영업 상태 실시간 반영·폐업 식당 추천 제외 필터 | `06:2-1 출력측 검사 W-1`(마지막 행에 자리만 둠) | **X** |

**집계 — O 29건 · X 3건(32건 중 90.6% 반영).**

### X 3건의 사유

| # | 점검 항목 | 사유 | 무엇을 해야 닫히나 |
|---|----------|------|-----------------|
| 26 | 전송요구권 대응·내보내기 이력 | 내보내기를 **실행하는 단계가 ③에 없어** 기록 항목을 걸 자리가 없음. 단계ID를 만들 수 있는 것은 ③뿐이라 ⑥이 `변경요청` 1행으로 되돌렸고 자동 반영하지 않음 | ③에 내보내기 워크플로우 또는 단계를 신설한 뒤 ⑥이 기록 항목을 붙임 |
| 30 | 위치정보 보호 책임자 지정 | 요구는 US:NFR-SYS-040 체크리스트에 있으나 **책임자의 역할·연락 창구가 원문에 없음**. ⑦이 칸을 만들고 `[확인필요: 위치정보 보호 책임자 지정]`으로 남김 | 법무·컴플라이언스가 책임 주체를 지정하면 ⑦의 운영 책임 주체 칸이 채워짐 |
| 32 | 영업 상태 필터 | 판정 원천(식약처 공공 API)이 **연동 확정 문장이 아니라 "필요하다"로 끝남**(`V-04` 20번). 라벨 목록의 값을 채울 근거가 없어 ⑥이 자리만 두고 값을 비움 | 식약처 공공 API 연동을 확정하면 ⑤가 커넥터를, ⑥이 라벨 목록을 채움 |

---

## 2. 제외 범위 미포함 대조표

`V-09` 범위 제외 11항목을 1행씩 대조함. `미포함 확인 위치`는 그 항목이 설계서에서 **값으로 쓰이지
않았음**을 확인한 자리임.

| 제외 항목 | 미포함 확인 위치(파일명:섹션명) | 확인 결과 |
|----------|---------------------------|:--------:|
| 1 팀 점심 합의(그룹 투표·실시간 동기화·초대 링크) | `02-논리아키텍처.md:판정 기록 2 — 노드 수집 결과`(참여 주체에 그룹 세션 없음) · `01-목표품질속성카드.md:미대응 Pain Point`(팀 갈등 없는 합의 1행) · `07:부속 표 대조 기록` | O |
| 2 영양·식단 관리(영양 DB 구축·식약처 연동·건강 데이터 처리) | `05-지식도구설계.md:1-1 정형 접근 경로`(영양 원천 행 0건) · `01:성격 선언`(고영향 `다` 목 판정 근거) | O |
| 3 알레르기 안전망 A35(식당·메뉴 원재료 교차 검증) | `05:1-1 정형 접근 경로`(`V-04` 24번을 원천으로 승격하지 않음 — `경로 불가` 판정) · `01:미확정 항목`(알레르겐 판정 데이터 원천) | O |
| 4 리뷰 텍스트 수집·입력 | `02:구조 — 전체 관계도`(입력 라벨이 고정 라벨 + 키워드 1개) · `06:2-1 출력측 검사 W-3`(라벨 목록 강제) · `05:1-2 비정형정보`(문서 코퍼스 0건 판정 근거) | O |
| 5 이미지 입력(사진 업로드·메뉴판 OCR·영수증 인식) | `02:구조 — 전체 관계도`(입력 모달리티가 텍스트·좌표·라벨) · `03-패턴시퀀스설계.md:워크플로우 표`(업로드 트리거 0건) | O |
| 6 음성 입력(STT)·음성 출력(TTS) | `02:구조 — 전체 관계도`(입출력 경계) · `03:워크플로우 표`(음성 트리거 0건) | O |
| 7 문서 RAG·GraphRAG | `05:1-2 비정형정보 — 경로 채택·미채택`(문서 코퍼스 검색 미채택 · GraphRAG 미채택) · `05:1-2 관계정보 스펙 8항목`(전부 `해당 없음`) | O |
| 8 NL2SQL(자연어 → SQL 생성) | `05:1-1 정형 접근 경로`(13경로 전건 `고정 쿼리`, NL2SQL 0건 — ③ `모델 호출` 열 `0회`가 근거) | O |
| 9 MCP 서버 · A2A(에이전트 간 위임) | `07-배포설계.md:저장소 배치 표 — MCP 핸들 보관 위치`(`해당 없음`, 판정 근거 1줄) · `03:판정 기록 — 패턴 판정`(핸드오프 탈락) | O |
| 10 엣지 추천 모델 캐싱(A20) · 무중단 모델 핫스왑 카나리(A39) | `07:되돌리기 — 단위별 되돌리기`(배포 절차를 `직전 판 교체` 1종으로 고정) · `07:부속 표 대조 기록` | O |
| 11 자체 식당·메뉴 DB 구축 | `05:1-1 정형 접근 경로`(식당·메뉴 표 0건, 외부 커넥터 `C-3`으로만 조회) · `02:판정 기록 3 — 신뢰 경계 TB-3` | O |

**집계 — O 11건 · X 0건(11건 중 100% 미포함 확인).**

X 행이 없으므로 사유를 덧붙일 행도 없음.

---

## 3. 산출물 8건 목록

| 산출물 | 파일명 | 담당 |
|--------|--------|------|
| ① 목표/품질 카드 | `01-목표품질속성카드.md` | 클로니(직접) |
| ② 논리아키텍처 | `02-논리아키텍처.md` | 클로니(직접) |
| ③ 패턴/시퀀스 | `03-패턴시퀀스설계.md` | 플로니(Agent 위임) |
| ④ 역할계약서 | `04-에이전트역할계약서.md` | 플로니(Agent 위임) |
| ⑤ 지식/도구 | `05-지식도구설계.md` | 지식니(Agent 위임) |
| ⑥ 가드레일/관측 | `06-가드레일관측설계.md` | 커넥니(Agent 위임) |
| ⑦ 배포 | `07-배포설계.md` | 커넥니(Agent 위임) |
| 반영대조표 | `00-반영대조표.md` | 클로니(직접) |

공용 입력 1건 — `_shared/프로젝트정보.md`(변수 12종).

---

## 4. 되돌려 보낸 변경요청 종합 — 자동 반영하지 않은 17건

앞 산출물로 값을 되돌리는 것은 `변경요청` 1행으로만 하고 그 자리에서 고치지 않는 규칙을 따랐음.
아래는 사용자 판단이 필요한 목록이며 **이번 실행에서 자동 반영한 것은 ③ → ① 회신 1건뿐**임.

| 보낸 곳 | 받는 곳 | 건수 | 무엇 |
|--------|--------|:----:|------|
| ③ | ① · `_shared/프로젝트정보.md` | 5 | 화면 3초·API 3초 예산 분리 · 최악 경로 총량 목표 신설 · 식사 기록 200ms 기준의 축 명시 · 배치·이벤트 목표 신설 · LLM 폴백 타임아웃 1,200ms 확정(`V-05` 미확정 10번 닫음) |
| ④ | ③ | 2 | `S-B23` 인간 개입 표기(승인자 자기 참조) · `W-3`에 이력업무 요청 접수 단계 2개 신설 |
| ⑤ | ④ · ③ · ② | 4 | `R-3`에 `S-2` 추가 · `R-5`에 `S-1` 추가 · `K-57`에서 발송 본문 검사값 분리 · `S-5`/`S-7`에 기억 제한 안내 이력 보완 |
| ⑥ | ③ · `_shared/프로젝트정보.md` | 4 | `W-7` 반복 상한 ↔ 실행당 금액 불일치 · `W-8` 1일 발송 상한 건수 미기재 · `V-05` 10원/건 ↔ 실단가 어긋남 · 개인정보 내보내기 실행 단계 부재 |
| ⑦ | ② | 2 | 프로세스 분리표에 알림 발송 배치 소속 추가 · 저장소 목록에 추천 캐시 종류 구분 명기 |

**②의 `변경요청`은 0건**임(① 목표/품질 카드로 되돌릴 값이 없음).
`⑤ → ④` 되돌림은 규칙대로 ④를 갱신하지 않고 보고만 함 — ④는 논리 이름과 등급까지만 정하므로
④에 되짚어 넣을 값이 없음.

---

## 5. `[확인필요]` 종합 — 61건

| 산출물 | 건수 | 성격 |
|--------|:----:|------|
| ① 목표/품질 카드 | 4 | 예산 분리 기준 · 학습 반영률 목표값 · 고영향 시행령 카목 대조 · 알레르겐 판정 데이터 원천 |
| ② 논리아키텍처 | 10 | 외부 제공자 3종(그중 4종은 ⑦에서 사용자 확정으로 닫힘) · 저장소 보존 기간 4건 · 배치 소속 · 탈퇴 삭제 범위 · 영업 상태 원천 |
| ③ 패턴/시퀀스 | 13 | 예산 계열 8 · 단말 타임아웃 1 · 원천 2 · 그 외 2 |
| ④ 역할계약서 | 3 | 학습 반영률 목표값 · 상세·인사이트 응답 목표 · 사람 개입 운영 대응 창 |
| ⑤ 지식/도구 | 23 | 외부 API 명세 미대조 · 인덱스 제품 선언 방법 · 보존 기간 · 원천 5건 등 |
| ⑥ 가드레일/관측 | 15 | 단가 전제 4 · 제품 3 · 보존 기간 2 · 원천·요구 부재 6 |
| ⑦ 배포 | 15 | 제품 미정 8 · 보존 기간 4 · 리전 1 · 삭제 범위 1 · 보호 책임자 1 |

**합계 61건** — 산출물별 미확정 항목 표 행 수의 합과 같음. 항목이 겹치는 것은 각 산출물이
막히는 지점이 달라 따로 셌음(예: 알레르겐 판정 데이터 원천은 ①·⑤·⑥에서 각각 다른 칸을 막음).

**가장 넓게 막는 3건** — ⓐ 알레르겐 판정 데이터 원천(①의 Q-3 측정 · ⑤의 하드 필터 · ⑥의 차단 규칙)
ⓑ 회원 탈퇴·계정 삭제 요구사항 부재(②의 `S-1` · ⑥의 보존 정책 · ⑦의 삭제 범위)
ⓒ 예산 정본(`NFR-SYS-010` 200ms vs 3초)(①의 G-1 · ③의 8개 워크플로우 예산 대조).
